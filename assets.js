// const path = require('path');                        // ? node API, loads a module, what module is path?/what does it do?         
// const static = require('express-static');
// const router = require("express").Router();

// const WEBPACK_BASE_URL = 'http://localhost:5001';

// if (process.env.NODE_ENV == 'production') {
//     router.use(static(path.join(__dirname, 'dist')));
// } else {
//     router.all('/*', function (req, res) {
//         const newUrl = req.originalUrl.replace(/^\/dist/, '');
//         res.redirect(`${WEBPACK_BASE_URL}${newUrl}`);
//     });
// }

// module.exports = router;